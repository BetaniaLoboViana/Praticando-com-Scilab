// Operações matemáticas simples
a = 5;
b = 3;

soma = a + b;        // Soma
subtracao = a - b;   // Subtração
multiplicacao = a * b; // Multiplicação
divisao = a / b;     // Divisão
exponencial = a^b;   

disp(soma); 
disp(subtracao); 
disp(multiplicacao); 
disp(divisao); 
disp(exponencial); 
